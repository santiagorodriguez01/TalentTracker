# Club Frontend (Next 15 + MUI v6 + React Query + Zustand + RHF/Zod)

## Instalación
```bash
npm i
cp .env.local.example .env.local
npm run dev
```

Asegurate de tener el backend levantado en `NEXT_PUBLIC_API_URL`.

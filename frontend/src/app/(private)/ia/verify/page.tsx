'use client';
import { Box, Typography } from '@mui/material';

export default function IAVerifyPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Verificación IA</Typography>
      <Typography variant="body1" color="text.secondary" sx={{ mt: 2 }}>
        Página en construcción
      </Typography>
    </Box>
  );
}
